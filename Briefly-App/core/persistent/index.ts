import * as UserAuth from "./userAuth";
import * as Bookmarked from "./bookmarked";
import * as UserCredentials from "./userCredentials";

export default {
  UserAuth,
  Bookmarked,
  UserCredentials,
};
